# 导入与导出

## 导出简历

点击工具栏中的 **Export** 即可将整份简历下载为 JSON 文件。导出内容包括所有分区、项目、设置和自定义分区。可用于备份或将简历迁移到另一个实例。

## 导入数据

点击 **Import** 并选择之前导出的 JSON 文件。此操作会用导入的数据替换当前的简历数据。自定义分区和所有设置均包含在内。

!!! tip
    导入前请先导出当前简历，以便保留当前状态的备份。
